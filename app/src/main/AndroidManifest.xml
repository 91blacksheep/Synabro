<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.first">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Loading.Client.CLoadingClient" />
        <activity android:name=".Home.CHomeClient" />
        <activity android:name=".Emotion.Client.CEmotionClient" />
        <activity android:name=".Setting.CSettingClient" />
        <activity android:name=".StoryList.Client.CStoryListClient" />
        <activity android:name=".StoryPlayer.Client.CStoryPlayerClient" />
        <activity android:name=".UiSetting.CMyText" />
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

</manifest>
